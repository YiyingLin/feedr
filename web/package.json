{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "js-cookie": "^2.2.0",
    "lodash": "^4.17.4",
    "material-ui": "^0.19.4",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "request": "^2.83.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf ../src/main/resources/public/ && mkdir -p ../src/main/resources/public/ && mv -v build/* ../src/main/resources/public/ && rmdir build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
